# Ralph Progress Log
Started: Mon, Jul 20, 2026  2:04:29 PM
---

## Codebase Patterns
- Backend lives in `backend/` (Python/FastAPI + SQLAlchemy 2.0 + Alembic + PostgreSQL). Repo root has no top-level src; each subsystem gets its own folder (`backend/`, later `frontend/`).
- Python venv is at `backend/.venv` (gitignored). Activate with `source backend/.venv/Scripts/activate` (Git Bash on Windows) before running pip/alembic/ruff/mypy.
- DB config is env-driven: `backend/app/config.py` reads `DATABASE_URL` from `backend/.env` (gitignored; `.env.example` is committed as the template). Local Postgres 18 is running as a Windows service; `postgres`/`postgres` is the local dev superuser login (databases: `finance_invoice_approval` for dev, `finance_invoice_approval_test` for tests).
- SQLAlchemy models live in `backend/app/models.py` on a shared `Base` from `backend/app/database.py`. Alembic's `migrations/env.py` imports `app.models` and sets `target_metadata = Base.metadata` so `alembic revision --autogenerate` picks up model changes automatically. `env.py` also overrides `sqlalchemy.url` from `app.config.settings` at runtime, so `alembic.ini`'s `sqlalchemy.url` is intentionally left blank.
- Gotcha: Postgres ENUM columns (SQLAlchemy `Enum`) are NOT dropped automatically by `op.drop_table` when the enum is shared across multiple tables (autogenerate's downgrade only drops tables). Any migration that creates a `sa.Enum` must add explicit `sa.Enum(name='...').drop(op.get_bind(), checkfirst=False)` calls at the end of `downgrade()`, after all tables using that enum are dropped — otherwise re-running upgrade after downgrade fails with `DuplicateObject: type "..." already exists`. Verified by round-tripping upgrade -> downgrade -> upgrade.
- Lint/typecheck tooling: `backend/requirements-dev.txt` adds `ruff` and `mypy` on top of `requirements.txt`. Run `ruff check app migrations` and `mypy app` from `backend/` (with venv active) as the "typecheck/lint passes" gate for backend stories.
- Enum/status defaults: model-level Python `default=` (SQLAlchemy-side) is paired with `server_default=` (DB-side) so both ORM inserts and raw SQL/direct DB writes get the same default (e.g. `User.role` defaults to `requester`, `Request.status` defaults to `pending`).
- App structure: `backend/app/main.py` is the FastAPI entrypoint (`uvicorn app.main:app`); route handlers live in `backend/app/routers/<name>.py` as `APIRouter` instances included from `main.py`; cross-cutting business logic (email, file storage, etc.) goes in `backend/app/services/<name>.py`. `app/models.py` stays SQLAlchemy-only.
- `backend/app/config.py`'s `Settings` class is the single source of truth for all env-driven config (DB URL, SMTP, upload dir, file size/count limits) — add new settings there with an `os.getenv(..., default)` fallback, and mirror the key in both `.env` and `.env.example`.
- Auth stack: password hashing uses `bcrypt` directly (`app/security.py`: `hash_password`/`verify_password`), JWTs use `pyjwt` (`create_access_token`/`decode_access_token`), signed with `settings.jwt_secret_key`/`jwt_algorithm`/`jwt_expire_minutes` (env-driven, added to config.py). Pydantic request/response models live in `app/schemas.py` (uses `EmailStr`, which requires the `email-validator` package alongside `pydantic`).
- Test setup: pytest + httpx (for FastAPI's `TestClient`) live in `requirements-dev.txt`. `backend/tests/conftest.py` points at a separate `finance_invoice_approval_test` Postgres DB (override via `TEST_DATABASE_URL` env var), creates all tables once per test session via `Base.metadata.create_all`, and gives each test function an isolated DB transaction (`db_session` fixture: begin -> yield -> rollback -> close) so tests never leak data between each other. The `client` fixture overrides FastAPI's `get_db` dependency with `db_session` and returns a `TestClient`. Run tests with `pytest` from `backend/` (venv active) as the "Tests pass" gate.
- Gotcha: `.env` (gitignored, local-only) can get modified outside of an iteration (e.g. by the user or a hook rewriting SMTP addresses) between when you edit it and when you check it later — always re-read `.env` right before relying on it if some time has passed, and re-apply any settings your story added rather than assuming your earlier edit still stands. This never affects commits since `.env` isn't tracked.
- Auth/RBAC dependencies live in `backend/app/dependencies.py`: `get_current_user` (FastAPI dependency, reads `Authorization: Bearer <jwt>` via `fastapi.security.HTTPBearer(auto_error=False)`, decodes with `app/security.py`'s `decode_access_token`, looks up the user by the `sub` claim, raises 401 on missing/invalid/expired token or unknown user id) and `require_finance_user` (wraps `get_current_user`, raises 403 unless `role == UserRole.finance`). Future protected routes should depend on these via `Depends(get_current_user)` / `Depends(require_finance_user)` rather than re-implementing auth checks. Because FastAPI `Depends(...)` default args are plain function defaults, these dependency functions can also be called directly (not just through FastAPI's DI) in unit tests, passing an `HTTPAuthorizationCredentials` or `User` instance by hand — no need to spin up test endpoints just to unit test a dependency.
- File upload endpoints need `python-multipart` in `requirements.txt` (FastAPI raises at import/runtime otherwise for any `UploadFile`/`File(...)` param). File validation/storage logic lives in `backend/app/services/storage.py` (`validate_upload_batch` checks count/type/size and reads all file contents up front — raising before anything is written keeps an upload batch all-or-nothing; `save_request_document` writes to `{settings.upload_dir}/{request_id}/{filename}`). Tests that touch file storage should `monkeypatch.setattr(settings, "upload_dir", str(tmp_path))` (autouse fixture) so they never write into the real `backend/uploads/` dir.
- There's no endpoint to create a `finance`-role user (signup always defaults to `requester`) — tests needing a finance user must insert one directly via the `db_session` fixture (`User(..., role=UserRole.finance)`) and mint a token with `create_access_token(subject=str(user.id))`, same pattern as `tests/test_dependencies.py`.
---

## Mon, Jul 20, 2026 - US-001
- Implemented the core PostgreSQL schema and Alembic migration setup for the Finance Invoice Approval Platform: `users`, `requests`, `request_documents`, and `request_status_history` tables, matching all four AC field lists exactly (including FKs, unique email index, and status/role enums with defaults).
- Files changed: `backend/requirements.txt`, `backend/requirements-dev.txt`, `backend/.env.example`, `backend/.gitignore`, `backend/app/__init__.py`, `backend/app/config.py`, `backend/app/database.py`, `backend/app/models.py`, `backend/alembic.ini`, `backend/migrations/env.py`, `backend/migrations/versions/8f1622b03393_create_core_tables.py`.
- Verified: created local Postgres databases `finance_invoice_approval` (dev) and `finance_invoice_approval_test` (test); ran `alembic upgrade head` cleanly against both, confirmed table/column/FK shapes via `psql \d`, round-tripped `upgrade -> downgrade -> upgrade` successfully, and confirmed a follow-up `alembic revision --autogenerate` produces an empty diff (models match the migration exactly). `ruff check` and `mypy` both pass with zero issues.
- **Learnings for future iterations:**
  - See "Codebase Patterns" at the top of this file for the enum-drop gotcha, env/config wiring, and lint/typecheck commands — read that section before starting US-002 onward.
  - No FastAPI app exists yet (that's US-002) — only the SQLAlchemy models/migrations layer. US-002 should build the app package around `backend/app/database.py`'s `get_db()` dependency and `backend/app/config.py`'s `settings`.
  - `backend/.env` (real, gitignored) already points at `finance_invoice_approval`; a second `finance_invoice_approval_test` DB exists for future test-suite use (e.g. pytest fixtures) but nothing wires to it yet.
---

## Mon, Jul 20, 2026 - US-002
- Implemented the FastAPI project scaffold: `backend/app/main.py` (app entrypoint, includes routers), `backend/app/routers/health.py` (GET /health -> 200 {"status": "ok"}), empty `backend/app/routers/__init__.py` and `backend/app/services/__init__.py` packages for future route/service modules. Expanded `backend/app/config.py`'s `Settings` with SMTP host/port/username/password/from-email/use-tls, finance notification email, upload directory, and max file size/count limits, all env-driven with sane defaults; mirrored the new keys into `backend/.env.example` and `backend/.env`. Added `fastapi==0.115.6` and `uvicorn[standard]==0.34.0` to `requirements.txt`.
- Files changed: `backend/app/main.py` (new), `backend/app/routers/__init__.py` (new), `backend/app/routers/health.py` (new), `backend/app/services/__init__.py` (new), `backend/app/config.py`, `backend/.env.example`, `backend/.env`, `backend/requirements.txt`.
- Verified: ran `uvicorn app.main:app --port 8123` locally and confirmed `curl http://127.0.0.1:8123/health` returns `200 {"status":"ok"}`. `ruff check app migrations` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - See updated "Codebase Patterns" at the top of this file for the routers/services module convention and the config-settings convention — both apply starting with US-003.
  - US-003 (auth endpoints) should add `backend/app/routers/auth.py` and a password-hashing/JWT helper likely under `backend/app/services/` or a new `app/auth.py` core module; no auth dependency exists yet (that's US-004).
  - No pytest setup exists yet — US-003 is the first story requiring "Tests pass", so it should also add pytest + a test DB fixture wired to the existing `finance_invoice_approval_test` database (see US-001 learnings) and a `requirements-dev.txt` entry for `pytest`/`httpx` (TestClient dependency) if not already pulled in transitively by fastapi.
---

## Mon, Jul 20, 2026 - US-003
- Implemented POST /auth/signup and POST /auth/login. Signup hashes the password with bcrypt and inserts a `User` with default role `requester`, rejecting duplicate emails with 409. Login verifies credentials with bcrypt and returns a JWT access token (pyjwt, HS256, expiry from `JWT_EXPIRE_MINUTES`); invalid credentials (wrong password or unknown email) both return the same generic 401 "Incorrect email or password" so email existence isn't leaked.
- Files changed: `backend/app/security.py` (new: bcrypt hashing + JWT create/decode helpers), `backend/app/schemas.py` (new: `SignupRequest`/`LoginRequest`/`UserResponse`/`TokenResponse` Pydantic models), `backend/app/routers/auth.py` (new), `backend/app/main.py` (registers auth router), `backend/app/config.py` (added `jwt_secret_key`/`jwt_algorithm`/`jwt_expire_minutes`), `backend/.env` / `backend/.env.example` (JWT_* keys), `backend/requirements.txt` (bcrypt, pyjwt, email-validator), `backend/requirements-dev.txt` (pytest, httpx, pytest-dotenv), `backend/tests/conftest.py` (new: test DB fixtures), `backend/tests/test_auth.py` (new: 5 tests covering signup/login happy and error paths).
- Verified: `pytest tests/` → 5 passed against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - See updated "Codebase Patterns" at top of this file for the auth/security module layout and the pytest fixture pattern — reuse `client`/`db_session` fixtures from `tests/conftest.py` for all future endpoint tests rather than building new DB setup per test file.
  - US-004 (auth dependency/RBAC) should build a `get_current_user` FastAPI dependency in `app/security.py` (or a new `app/dependencies.py`) that calls `decode_access_token`, looks up the user by the `sub` claim (currently the user's `id` as a string), and raises 401 on missing/invalid/expired tokens; a second dependency should wrap it and raise 403 when `user.role != UserRole.finance`.
  - `UserResponse`/other response schemas use Pydantic's `from_attributes=True` (v2 style, replaces old `orm_mode`) to serialize SQLAlchemy model instances directly.
---

## Mon, Jul 20, 2026 - US-004
- Implemented `backend/app/dependencies.py` with `get_current_user` (FastAPI dependency: validates the JWT from the `Authorization: Bearer` header via `fastapi.security.HTTPBearer(auto_error=False)`, decodes it with `app/security.py`'s `decode_access_token`, loads the user by the `sub` claim, raises 401 if the header is missing, the token is invalid/expired, or the user id doesn't exist) and `require_finance_user` (wraps `get_current_user`, raises 403 for non-`finance` roles).
- Files changed: `backend/app/dependencies.py` (new), `backend/tests/test_dependencies.py` (new: 6 tests covering valid/missing/invalid/unknown-user tokens and finance/requester RBAC).
- Verified: `pytest tests/` → 11 passed (5 existing auth tests + 6 new dependency tests) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - See updated "Codebase Patterns" at top of this file for the `get_current_user`/`require_finance_user` dependency pattern — reuse these via `Depends(...)` for every protected endpoint from US-005 onward instead of re-implementing auth/RBAC checks per router.
  - Dependency functions with `Depends(...)` default args can be unit tested by calling them directly with hand-built arguments (e.g. `HTTPAuthorizationCredentials(scheme="Bearer", credentials=token)`, a `User` instance, or the `db_session` fixture) — no need for dedicated test-only endpoints.
---

## Mon, Jul 20, 2026 - US-005
- Implemented `POST /requests`: accepts `amount`/`currency`/`category`/`expense_date`/`description` from the authenticated requester (via `Depends(get_current_user)`), inserts a `Request` row with `status='pending'` and `requester_id` set to the logged-in user, and returns the created request (id, status, and full fields) with 201. Missing/invalid fields are rejected by Pydantic validation (422) via `RequestCreate` (amount must be > 0, currency/category/description non-empty).
- Files changed: `backend/app/schemas.py` (added `RequestCreate`/`RequestResponse`), `backend/app/routers/requests.py` (new), `backend/app/main.py` (registers requests router), `backend/tests/test_requests.py` (new: 3 tests covering happy path, missing required field, and unauthenticated request).
- Verified: `pytest tests/` → 14 passed (11 existing + 3 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - New protected endpoints follow the pattern: `Depends(get_current_user)` for the current user, `Depends(get_db)` for the session — see `app/routers/requests.py` for a template of a simple authenticated POST endpoint that inserts and returns a model.
  - `RequestResponse` uses `from_attributes=True` like `UserResponse`; `Decimal` fields serialize as JSON strings (e.g. `"125.50"`) through Pydantic — tests should compare against string amounts, not floats.
  - US-006 (document upload) will add `POST /requests/{id}/documents` and `GET /requests/{id}/documents/{doc_id}` to the same `app/routers/requests.py` router; it needs the `upload_dir`/max file size/count settings already in `app/config.py` (from US-002) and should verify the request is owned by the current user (404/403 for others) before accepting uploads.
---

## Mon, Jul 20, 2026 - US-006
- Implemented `POST /requests/{id}/documents` (multi-file upload, owner-only, validates file type against PDF/JPG/PNG extensions, size against `MAX_FILE_SIZE_MB`, and count against `MAX_FILES_PER_REQUEST`, saving accepted files to `{UPLOAD_DIR}/{request_id}/{filename}` and inserting `request_documents` rows) and `GET /requests/{id}/documents/{doc_id}` (returns the file via `FileResponse`, restricted to the request's owner or a finance user, 404 for unknown request/doc).
- Files changed: `backend/app/services/storage.py` (new: `validate_upload_batch` + `save_request_document`), `backend/app/schemas.py` (added `RequestDocumentResponse`), `backend/app/routers/requests.py` (added the two endpoints + a shared `_get_request_or_404` helper), `backend/requirements.txt` (added `python-multipart`, required by FastAPI for file uploads), `backend/tests/test_documents.py` (new: 11 tests covering upload success/multi-file, type/size/count rejection, non-owner rejection, download success for owner/finance, download rejection for non-owner/non-finance, and 404 for unknown doc).
- Verified: `pytest tests/` → 24 passed (13 existing + 11 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - See updated "Codebase Patterns" at top of this file for the file-upload/storage-service pattern, the `python-multipart` dependency gotcha, and the finance-test-user pattern — reuse these for US-007 (email) and any later document-touching stories.
  - Upload validation is all-or-nothing per request: `validate_upload_batch` reads every file's content and checks type/size/count before any file is written to disk, so a batch either fully succeeds or fails with no partial writes/DB rows.
  - US-007 (email notification on request creation) should add a `backend/app/services/email.py` utility using the existing SMTP settings in `app/config.py`, called from `create_request` in `app/routers/requests.py`; failures must be caught/logged there so they never raise or block the 201 response, and tests should mock the email-sending call rather than hitting real SMTP.
---

## Mon, Jul 20, 2026 - US-007
- Implemented `backend/app/services/email.py`: `send_email(to_email, subject, body)` is a thin `smtplib.SMTP` wrapper (STARTTLS if `SMTP_USE_TLS`, login only if `SMTP_USERNAME` set) using the SMTP settings already in `app/config.py`; `send_new_request_notification(...)` builds the finance-facing message (requester name, amount, currency, category, request id) and wraps `send_email` in a try/except that logs (`logger.exception`) and swallows any error so it never propagates. Wired into `create_request` in `app/routers/requests.py`: called after `db.commit()`/`db.refresh()`, right before returning the response.
- Files changed: `backend/app/services/email.py` (new), `backend/app/routers/requests.py` (import + call in `create_request`), `backend/tests/test_email.py` (new: 5 tests — SMTP call happens, notification helper swallows a raised send_email error, `create_request` calls the notification helper with correct kwargs (mocked), and request creation still returns 201 even when the underlying SMTP call itself throws).
- Verified: `pytest tests/` → 28 passed (23 existing + 5 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - Email-sending pattern: a low-level `send_email` (raises on failure, unit-testable by mocking `app.services.email.smtplib.SMTP`) plus a higher-level per-use-case function (`send_new_request_notification`) that catches all exceptions and logs — never let email errors bubble into the request/response cycle. US-012 (status-change email notification) should follow this same two-layer shape: add a `send_status_change_notification(...)`-style function in `app/services/email.py` and call it from the US-011 review endpoint the same way `create_request` calls `send_new_request_notification`.
  - To assert a router called a service function without caring about the service's internals, patch the function where it's *imported into* the router module (e.g. `patch("app.routers.requests.send_new_request_notification")`), not where it's defined — this is the pattern used in `test_create_request_sends_notification_email`.
  - `Request.amount` is a SQLAlchemy `Numeric` (Python `Decimal`); cast with `str(request.amount)` when interpolating into an email body/subject.
- Route ordering gotcha: FastAPI/Starlette matches path routes in registration order and does NOT use the Python type hint (e.g. `request_id: int`) as a routing-time constraint — it's applied only after a route's raw path template matches. So a literal path like `GET /requests/mine` must be registered *before* a parameterized sibling like `GET /requests/{request_id}` in the same router, or `/requests/mine` will match `{request_id}` first and fail path-param conversion. Apply this ordering rule to every new literal route added under `app/routers/requests.py` (e.g. future `/requests/pending` in US-009 must also be registered before `/{request_id}`).
- The `Request.status_history` relationship now has `order_by="RequestStatusHistory.changed_at"` in `app/models.py`, so any endpoint touching `request.status_history` (via ORM) gets chronological order for free — no need to sort manually in each router.
- Test gotcha: the `db_session` fixture wraps each test in one outer connection-level transaction, and Postgres's `func.now()` returns the *same* value for every statement within one transaction. Two rows inserted back-to-back via the API in the same test can get identical `created_at`/`updated_at` timestamps — don't assert relative ordering by creating two rows and comparing insertion order; instead force a deterministic timestamp via `db_session` (fetch the row, set `.updated_at` explicitly, commit) before asserting sort order. See `test_list_my_requests_sorted_by_most_recently_updated` in `tests/test_request_views.py`.
---

## Mon, Jul 20, 2026 - US-008
- Implemented `GET /requests/mine` (returns only the logged-in user's requests, sorted by `updated_at` descending) and `GET /requests/{request_id}` (returns full request details plus documents and full status history, restricted to the request's owner or a finance user; 403 for others, 404 for unknown id). Added `RequestStatusHistoryResponse` and `RequestDetailResponse` (extends `RequestResponse` with `documents`/`status_history` lists) to `app/schemas.py`. Added `order_by="RequestStatusHistory.changed_at"` to the `Request.status_history` relationship in `app/models.py` so status history is chronological wherever it's accessed via the ORM.
- Files changed: `backend/app/models.py`, `backend/app/schemas.py`, `backend/app/routers/requests.py`, `backend/tests/test_request_views.py` (new: 7 tests covering own-requests filtering/sorting/auth, and detail view success for owner/finance, 403 for others, 404 for unknown request).
- Verified: `pytest tests/` → 35 passed (28 existing + 7 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - See updated "Codebase Patterns" at top of this file for the route-registration-order gotcha (literal routes before parameterized siblings) and the `func.now()`-in-one-transaction test gotcha — both are directly relevant to US-009 (`/requests/pending`) and US-010 (`/requests` with filters), which add more literal/parameterized siblings to the same router.
  - US-009's `GET /requests/pending` must be registered before `GET /requests/{request_id}` for the same reason `/requests/mine` was; US-010's `GET /requests` (no path param) doesn't conflict since it has no shared prefix with `{request_id}`.
---

## Mon, Jul 20, 2026 - US-009
- Implemented `GET /requests/pending` (finance-only, via `Depends(require_finance_user)`): returns all requests with status `pending` or `more_info_needed` across all requesters, sorted by `created_at` descending, each item including requester name/email alongside the standard request fields (amount, currency, category, description, expense_date, status, created_at). Registered before `GET /requests/{request_id}` per the existing route-ordering pattern.
- Files changed: `backend/app/schemas.py` (added `RequestWithRequesterResponse extends RequestResponse` with `requester_name`/`requester_email`), `backend/app/routers/requests.py` (new `list_pending_requests` endpoint), `backend/tests/test_finance_requests.py` (new: 4 tests covering pending+more_info_needed inclusion with requester fields, exclusion of approved/rejected, non-finance 403, and unauthenticated 401).
- Verified: `pytest tests/` → 39 passed (35 existing + 4 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - Pattern for response schemas that join in a related model's fields (e.g. requester name/email onto a request): since Pydantic's `from_attributes=True` only does plain `getattr` per field (it won't traverse `request.requester.name` for a field named `requester_name`), build the response explicitly in the router: `RequestWithRequesterResponse(**RequestResponse.model_validate(request).model_dump(), requester_name=request.requester.name, requester_email=request.requester.email)`. Reuse this shape for US-010's `GET /requests` (which also needs requester name/email per finance-facing listing conventions).
  - US-010 (`GET /requests` with filters) can reuse `RequestWithRequesterResponse` and `require_finance_user` directly; query params (`requester_id`, `status`, `category`, `date_from`, `date_to`) should be applied as optional `Query(None)` params with conditional `.filter(...)` calls chained onto the base `db.query(Request)`.
---

## Mon, Jul 20, 2026 - US-010
- Implemented `GET /requests` (finance-only, via `Depends(require_finance_user)`): returns all requests regardless of status, reusing `RequestWithRequesterResponse` (requester name/email joined in) just like `/requests/pending`. Supports optional query params `requester_id`, `status` (aliased from a `status_filter` param name since `status` collides with FastAPI's imported `status` module — resolved via Pydantic's `Query(None, alias="status")`), `category`, `date_from`, `date_to` (the latter two filtering on `expense_date`), each applied as a conditional `.filter(...)` chained onto `db.query(Request)`. Registered with path `""` (no path param), so it doesn't conflict with the existing `/{request_id}` ordering rule, but was placed before `/{request_id}` in the file for readability alongside `/pending`.
- Files changed: `backend/app/routers/requests.py` (new `list_all_requests` endpoint + `Query`/`date` imports), `backend/tests/test_all_requests.py` (new: 7 tests covering all-status inclusion, status filter, category filter, requester_id filter, date range filter, non-finance 403, unauthenticated 401).
- Verified: `pytest tests/` → 46 passed (39 existing + 7 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - Gotcha: a FastAPI query param named `status` shadows the `status` module imported for `status.HTTP_*` constants in the same file. Fix by naming the Python parameter something else (`status_filter`) and using `Query(None, alias="status")` so the wire/query-string name stays `status` while the Python identifier doesn't collide.
  - US-011 (finance review action endpoint) will add `POST /requests/{id}/review` to this same router — it can reuse `_get_request_or_404` and `require_finance_user`, and should follow the `request_status_history` append pattern (insert a `RequestStatusHistory` row with `changed_by_user_id=current_user.id`) alongside updating `request.status`.
---

## Mon, Jul 20, 2026 - US-011
- Implemented `POST /requests/{id}/review` (finance-only, via `Depends(require_finance_user)`): accepts `action` (`approve`|`reject`|`more_info`) and optional `reason`, maps the action to the corresponding `RequestStatus` (`approved`/`rejected`/`more_info_needed`) via a module-level `_REVIEW_ACTION_TO_STATUS` dict, updates `request.status`, and appends a `RequestStatusHistory` row (`status`, `reason`, `changed_by_user_id=current_user.id`). Returns the updated `RequestDetailResponse` (status + full status history) so callers see the new history entry immediately. Reason is required (raises `HTTPException(400)`, not a pydantic 422) when action is `reject` or `more_info`; optional for `approve`.
- Files changed: `backend/app/schemas.py` (added `ReviewActionType` enum + `ReviewRequest` schema; reason is plain `str | None` so the required-for-reject/more_info check is enforced manually in the router as an explicit 400, matching the file-upload validation style rather than relying on pydantic's 422), `backend/app/routers/requests.py` (new `review_request` endpoint + `_REVIEW_ACTION_TO_STATUS` mapping, imports `RequestStatusHistory`), `backend/tests/test_review.py` (new: 8 tests covering approve success, reject without/with reason, more_info without/with reason, non-finance 403, unauthenticated 401, unknown request 404).
- Verified: `pytest tests/` → 54 passed (46 existing + 8 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - Pattern: when an AC calls for "4xx/400 if a conditionally-required field is missing" alongside other pydantic-validated fields, keep the field optional in the Pydantic schema and raise `HTTPException(400, ...)` explicitly in the router rather than using a pydantic `model_validator` (which would produce a 422, not the 400 the AC expects). This mirrors the file-upload validation approach in `app/services/storage.py` (US-006).
  - US-012 (status-change email notification) should call a new `send_status_change_notification(...)` (to be added to `app/services/email.py`, same two-layer catch-and-log shape as `send_new_request_notification`) from inside `review_request` in `app/routers/requests.py`, after `db.commit()`/`db.refresh()`, passing the requester's email/name, the new status, and `payload.reason`.
---

## Mon, Jul 20, 2026 - US-012
- Implemented `send_status_change_notification(requester_email, request_id, new_status, reason)` in `app/services/email.py` (same two-layer shape as `send_new_request_notification`: builds a subject/body from the new status and optional reason, calls `send_email`, catches and logs any exception so it never propagates). Wired into `review_request` in `app/routers/requests.py`, called after `db.commit()`/`db.refresh()` with `request.requester.email`, `new_status.value`, and `payload.reason`.
- Files changed: `backend/app/services/email.py`, `backend/app/routers/requests.py`, `backend/tests/test_email.py` (added `test_send_status_change_notification_swallows_smtp_errors`), `backend/tests/test_review.py` (added `test_review_sends_status_change_notification` mocking `app.routers.requests.send_status_change_notification` and asserting call kwargs, and `test_review_succeeds_even_if_email_send_fails` patching `app.services.email.smtplib.SMTP` to raise and asserting the review endpoint still returns 200).
- Verified: `pytest tests/` -> 57 passed against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - Environment gotcha (not a code bug): running multiple pytest invocations concurrently against the shared `finance_invoice_approval_test` DB, or killing a pytest process mid-run (e.g. via `kill -9`), can corrupt the DB for subsequent runs two ways: (1) a killed process can leave a connection "idle in transaction" holding row/table locks that block later tests indefinitely (diagnose via `SELECT pid, state, query FROM pg_stat_activity WHERE datname='finance_invoice_approval_test'`, fix via `SELECT pg_terminate_backend(<pid>)`); (2) `tests/conftest.py`'s session-scoped `_create_test_schema` fixture calls `Base.metadata.create_all` at session start and `Base.metadata.drop_all` at session end — if two pytest sessions overlap, one session's teardown `drop_all` can drop tables out from under the other session's still-running tests (symptom: `sqlalchemy.exc.ProgrammingError: relation "users" does not exist`). Always run the backend test suite as a single foreground/background process at a time, never overlapping, and let it run to completion (a full clean run of all 57 tests can legitimately take ~3-4 minutes on this machine — don't assume a still-running pytest process is hung and kill it prematurely).
---

## Mon, Jul 20, 2026 - US-013
- Implemented `POST /requests/{id}/resubmit` (owner-only, via `Depends(get_current_user)`, multipart form): accepts optional `description` (`Form(None)`) and optional `files` (`File(default_factory=list)`, reusing `validate_upload_batch`/`save_request_document` from `app/services/storage.py` exactly like the document-upload endpoint, including the existing-count check against `max_files_per_request`). Only allowed when `request.status == RequestStatus.more_info_needed` (else 400); updates `description` if provided, adds any uploaded documents, sets `status` back to `pending`, and appends a `RequestStatusHistory` row (`status=pending`, `reason="Resubmitted by requester"`, `changed_by_user_id=current_user.id`). Returns the updated `RequestDetailResponse`.
- Files changed: `backend/app/routers/requests.py` (new `resubmit_request` endpoint + `Form` import), `backend/tests/test_resubmit.py` (new: 6 tests covering description update, document addition, rejection when status isn't `more_info_needed`, non-owner 403, unauthenticated 401, unknown request 404).
- Verified: `pytest tests/` -> 63 passed (57 existing + 6 new) against the `finance_invoice_approval_test` DB. `ruff check app migrations tests` and `mypy app` both pass with zero issues.
- **Learnings for future iterations:**
  - `UploadFile = File(default_factory=list)` (not `File(None)`/`File([])`) is the correct way to make a `list[UploadFile]` form field optional in FastAPI — an empty list means "no files uploaded," and `if files:` guards the whole validate/save block so a description-only resubmit never touches `validate_upload_batch`.
  - This is the last backend-only story in the PRD (US-014 onward are all frontend/UI stories using React) — future iterations should expect to scaffold a `frontend/` directory (React + routing) and treat the backend routers/schemas built so far (auth, requests, documents, review, resubmit) as the stable API surface to build the UI against. Re-read the "Codebase Patterns" section for the full list of existing endpoints before starting US-014.
---
